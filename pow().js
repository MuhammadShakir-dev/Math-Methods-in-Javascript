/*
                            pow() is used to find the power of the number.

                  */

                  var a = Math.pow(prompt("Enter a base value"), prompt("Enter a power value"));
                  document.write("<h1>The power of the number is: " + a + "</h1>");
