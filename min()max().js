          /*
                 min() and max() is used to find the minimum and maximum value.
                    min() is used to find the minimum value.
                    max() is used to find the maximum value.




          */
           var a  = Math.min(prompt("Enter first value or number"), prompt("Enter second value or number"), prompt("Enter third value or number"));
           document.write("<h1>Minimum value is: " + a + "</h1>");

           var a = Math.max(prompt("Enter the first number"), prompt("Enter the second number"), prompt("Enter the third number"));
          document.write("<h1>Maximum value is: " + a + "</h1>");
